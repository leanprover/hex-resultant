name: CI
on:
  push:
    branches: [main]
  pull_request:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgmp-dev libpari-dev pari-gp
      - name: Install oracle dependencies
        run: python3 -m pip install --user python-flint cypari2
      - uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
      - name: Build library and release regressions
        run: lake build HexResultant
      - name: Build conformance sidecar
        run: |
          cd conformance
          lake build
      - name: Conformance oracle
        run: |
          (cd conformance && lake exe hexresultant_emit_fixtures) > /tmp/hex-resultant-fresh.jsonl
          diff -u conformance-fixtures/HexResultant/resultant.jsonl /tmp/hex-resultant-fresh.jsonl
          python3 scripts/oracle/resultant_flint_pari.py < /tmp/hex-resultant-fresh.jsonl
      - name: Build and verify benchmarks
        env:
          BENCH_VERIFY_HARD_CAP_SECONDS: "360"
        run: |
          cd bench
          lake build hexresultant_bench
          bash ../scripts/ci/check_bench_verify_budget.sh hexresultant_bench
      - name: Assert Mathlib-free
        run: |
          if find .lake bench/.lake conformance/.lake -type d -name Mathlib 2>/dev/null | grep -q .; then
            echo "ERROR: a Lake build pulled in Mathlib"; exit 1; fi
          if grep -rqE '^import Mathlib' HexResultant conformance/HexResultant bench/HexResultant 2>/dev/null; then
            echo "ERROR: a source file imports Mathlib"; exit 1; fi
          echo "Mathlib-free OK"
